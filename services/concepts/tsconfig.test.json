{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "allowImportingTsExtensions": true,
    // The tests and the import script run under Node, driving the same SQL
    // through `node:sqlite`.
    "types": ["node"]
  },
  "include": ["src", "test", "scripts"]
}
