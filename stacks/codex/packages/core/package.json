{
  "name": "@agent-proxy/codex-core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure usage, pricing, sidecar, and reporting contracts for codex-proxy.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
