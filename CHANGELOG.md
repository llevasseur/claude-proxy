# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/),
and this project adheres to [Semantic Versioning](https://semver.org/).

This project has not cut a release yet, so everything below sits under
`[Unreleased]`, grouped by the date its pull request merged (newest first).

## [Unreleased]

### Added

- **The last scanning read paths join the substrate, and it turns out they needed no new tables** — slice 4 of the [SQLite migration](docs/wayfinder/map-sqlite-substrate.md) closes out the routes slices 1–3 left behind. `/api/skim`, `/api/skim/trend`, `/api/withheld` and the list half of `/api/sessions/suggestions/status` were the last builders still calling `readSidecars` and `listSessionGraphs` directly instead of going through the `SidecarSource` seam; all five (`buildSkim`, `buildSkimTrend`, `buildWithheld`, `buildSuggestionStatus` and `applySuggestionStatus`) now take a source and default to the file scan, and all four routes are registered with the parity harness and wired into shadow mode. The notable result is a **negative** one: not a single new table was needed. Every one of these is a different aggregation over the audit sidecars slice 1 indexed and the session graphs slice 2 indexed — a skim digest is those sidecars grouped by day, the withheld report is those sidecars cross-referenced against the device deny-list, and the suggestion-status list is slice 2's buckets joined to a flag file. The schema is unchanged at version 3, the standing invariants are untouched (`logs/` is still the sole source of truth, the database is still disposable and still rebuilt by `rm logs/claude-proxy.db && pnpm --filter server ingest`, `proxy/` is still not modified, reads are still file-backed, still `node:sqlite` and still **no new dependency**), and every response is still byte-for-byte what it was. Four of the names on slice 4's list are **deliberately out of scope** rather than forgotten, and the reason is the same boundary that kept `~/.claude/commands` unindexed in slice 3: `/api/projects`, `/api/jobs` and `/api/hooks-plugins` read `~/.claude/projects`, `~/.claude/jobs` and `~/.claude/settings.json`, which sit outside `logs/` — the thing [ADR 0004](docs/adrs/0004-adopt-sqlite-as-the-query-substrate.md) scopes the substrate to being a materialized view *of* — and are not reconstructible by re-ingesting anything, so indexing them would leave a disposable view holding the only copy of authored state. `/api/filters` is a static inventory that reads no disk at all. For the same reason `/api/sessions/suggestions/status` is registered for its derived half only: the bucket/suggestion join comes out of the indexed transcripts, while the flags stay in `logs/suggestion-status.json` and are read as a plain file on both sides of the comparison, so a disagreement there can only ever be a substrate bug. Two changes to the harness itself follow from what these routes read. `ParityContext` gained a `settingsPath`, pinned exactly the way `commandsDir` was in slice 3, so the replay does not depend on whichever `~/.claude/settings.json` the machine happens to have and cannot see it change between the two passes. And the real-corpus snapshot now takes the `.request.txt` bodies — `/api/skim` parses them for the last user turn, so leaving them out would have had both readers agree on `null` and proved nothing; they are write-once, so a hardlink genuinely freezes them and the snapshot still costs directory entries rather than the corpus. `logs/suggestion-status.json` joins `usage-live.json` in the same snapshot for the same reason, both being rewritten by temp-file-and-rename, which leaves the inode the snapshot holds untouched. The two window-shaped routes are replayed as of the newest archived day rather than as of every day: a window is an aggregate over per-day reads the harness already replays for every day via `/api/skim`, `/api/summary` and `/api/trends`, so running every window as of every day re-reads the corpus quadratically for coverage that is already there. The whole real-archive replay went from 83s to 101s. `NORMALIZATIONS` is still empty.
- **The command runs are indexed, and the three routes over them are proved byte-identical from SQL** — slice 3 of the [SQLite migration](docs/wayfinder/map-sqlite-substrate.md) extends the substrate from transcripts to `logs/commands/runs.jsonl`, the append-only store `reconcileCommandRuns` distils out of the sessions and the captured requests before they age out. Six tables land: `command_run` (one row per record, carrying the envelope, the model and the run window, the outcome / interruption / `reachedEnd` triple, and the nine `runTotals` figures flattened into columns), `command_run_flag` and `command_run_thread` (the leading `--flags` and the run's whole agent family, the latter joining back to slice 2's `session` rows), `command_run_turn` (the per-turn context series), `command_run_step` (the per-step tokens, cost and the five waste counters), and `command_run_pattern`. Everything slices 1 and 2 settled still holds and is not revisited: `logs/` remains the sole source of truth with the database a **disposable view** — `rm logs/claude-proxy.db && pnpm --filter server ingest` still rebuilds all of it — nothing authored goes in, `proxy/` is untouched, reads are still file-backed, and the whole thing is still `node:sqlite` with raw prepared statements and **no new dependency**. Two things about command runs forced departures from the previous slices' shape, and both are why runs are their own slice. First, **a run is stored, not re-derived**: unlike a sidecar or a transcript, nothing re-reads a run's source on every read, and `readCommandRuns` deliberately hands back records it does not fully understand — `isCommandRun` checks three identity fields, so a record written by a newer or older writer is kept and rendered from whatever it has, which is also why half the record's fields are optional (`retired?`, `CommandStep.artifacts?`, and every field `runTotals` and `toListItem` default). Rebuilding such a record from columns cannot reproduce "this key was absent" versus "this key held the default", and it would silently drop a future writer's fields the moment slice 5 flips reads to DB-backed. So `command_run` carries a `document` column holding the record itself: **the tables exist to be queried, the document is what is served**, and the DB-backed `readCommandRuns` re-parses documents and re-applies the store reader's own sort and `retired` filter rather than reassembling anything. Second, **the store is a single mutable file**, so it gets a third watermark shape — a `file_watermark` row keyed on the path, holding `bytes` + `modified` — and a store whose `stat` still matches is never opened; when it has moved, the whole file is re-parsed and every row replaced inside one transaction, which is bounded because the table holds one row per run rather than one per appended line. `logs/commands/` also gets its own `fs.watch`, since the watch on `logs/` is not recursive. The parity harness gains `/api/commands`, `/api/commands/command` (including one flag facet per command, so the filter is exercised and not just the aggregate) and `/api/commands/run`, all enumerated from the **file** side so the substrate is asked about every command and every run the store knows rather than only the ones it managed to index; the `/stream` variants are absent because SSE re-serves the same builder. The installed catalogue under `~/.claude/commands` is deliberately **not** indexed — it lives outside `logs/`, so it is not the substrate's to mirror and both backings read it identically — but the harness pins it on `ParityContext.commandsDir` so a `/sync` landing mid-run cannot move it between the two replays, and the real-corpus test **copies** both it and `runs.jsonl` into its snapshot, because a hardlink freezes which files exist and not their contents. Shadow mode covers the three new routes behind `SHADOW_DB`, computing the DB answer after the response is already written and never affecting it. `NORMALIZATIONS` is still empty.

- **The session transcripts are indexed too, and the ten routes over them are proved byte-identical from SQL** — slice 2 of the [SQLite migration](docs/wayfinder/map-sqlite-substrate.md) extends the substrate from audit sidecars to `logs/sessions/`: `session` (one row per transcript, carrying the header metadata `parseSessionTranscript` derives, the listing's `bytes` / `modified`, and the untruncated opening prompt off the `.state.json` sidecar), `session_node` (the appended step stream in transcript line order), and `session_node_text` (the `.nodes.jsonl` sidecar). Everything slice 1 settled still holds and is not revisited: `logs/` remains the sole source of truth with the database a **disposable view** — `rm logs/claude-proxy.db && pnpm --filter server ingest` rebuilds all of it — nothing authored goes in, `proxy/` is untouched, and the whole thing is still `node:sqlite` with raw prepared statements and **no new dependency**. The transcript **body** stays on disk exactly as before: `md_path` is a pointer, `readSession` still reads the file for its `content`, and the DB backing never stops those files being written, because [`/revive`](docs/adrs/0004-adopt-sqlite-as-the-query-substrate.md) reads them straight off disk with no API and no database. Two things about transcripts forced departures from slice 1's shape, and both are the point of doing sessions as their own slice. First, **a transcript is mutable** — the proxy appends to it for the life of a run — so "seen this filename stem already" is not a sufficient watermark the way it is for an immutable audit sidecar. The session watermark is therefore **per file** (`bytes` + `modified`): an append always moves the size, so one `stat` per transcript detects change without re-reading it, and ingest stays idempotent — a second pass over an unchanged directory parses nothing. The same mutability meant the `fs.watch` had to be widened, since the proxy appends inside `logs/sessions/` without touching `logs/` itself, and a non-recursive watcher on the parent would have let the tables trail the files for a whole run. Second, **`session_node_text` is deliberately not a column on `session_node`** and carries no foreign key to it: the sidecar is sparse, it outlives edits, and it can name a node index the transcript no longer has — the file reader hands such an entry back rather than dropping it, so the substrate has to be able to as well. Eviction, by contrast, is *not* a state here: unlike a `request` row, whose `.md` body can be retained away underneath it, a transcript **is** its row's source, so a body that disappears takes the row with it (and its nodes, by cascade) on the next pass. Ten more routes register with the **parity harness** — `/api/sessions`, `/api/sessions/graph`, `/api/sessions/session`, `/api/sessions/node-text`, `/api/sessions/breakdown`, `/api/sessions/errors`, `/api/sessions/graph/nodes`, `/api/sessions/suggestions`, `/api/sessions/suggestions/bucket` and `/api/context` — replayed both ways with the **full JSON** compared, key order included, and the `NORMALIZATIONS` list still **empty**: the agent tree is rebuilt on the DB side by the same `linkAgentSessions` the file reader uses, which sorts each family internally and so does not care what order SQL hands rows over in, and the listing sort is reproduced rather than compensated for. `/api/context/detail`, `/api/context/message` and `/api/context/tool` are the named exceptions and stay unregistered: they read a `.request.txt` body straight off disk and touch no indexed column, so there is no DB path for them to disagree on. Per-thread cases replay the newest twenty transcripts rather than all of them — not a weakened comparison, since `/api/sessions` and `/api/sessions/graph` are replayed whole and carry *every* transcript's metadata and node stream, so a row the substrate got wrong fails there regardless; the cap only bounds the routes that re-read request bodies per thread. The real-corpus test's snapshot was widened to take `sessions/` too — the transcripts being the most volatile thing in the log directory, they are precisely the race that snapshot exists to close — and it **copies** them rather than hardlinking, unlike the write-once audit sidecars: a hardlink shares the inode, so it freezes which files exist but not their contents, and the proxy's appends to a live transcript would otherwise land inside the snapshot mid-replay and read as a substrate mismatch. The session suffix list is kept separate from the sidecar one because `.md` means two different things in this tree and taking the request bodies would have changed which requests read as blob-evicted. **Shadow mode** covers all ten new routes on the same terms as before: served from the files, computed from SQL alongside, mismatch logged, off unless `SHADOW_DB` is set, and strictly unable to affect the response. **Nothing flips to DB-backed reads** — that is slice 5.

- **A SQLite query substrate over the logs, built as a disposable view and proved byte-identical before anything depends on it** — `logs/` is doc-shaped, so every read path in the server is a full `readdir` + `readFile` scan rebuilt per request; the questions the dashboard now asks ("which tools burn the most tokens across every session", "how does this move across thirty archived days") are joins and aggregations, and a directory listing cannot serve them without re-reading everything each time. So the audit sidecars are now indexed into **`logs/claude-proxy.db`** — SQLite in WAL mode via **`node:sqlite`**, which is built into Node and therefore adds **no dependency**, leaving the project's zero-dependency stance intact; the one real platform change is the engines floor moving from `>=18` to `>=22`, Node 18 having gone EOL in April 2025 and Node 20 in April 2026. Raw SQL with prepared statements, schema version in `PRAGMA user_version`, no ORM and no query builder. What the database is allowed to *be* is the load-bearing decision: it is a **disposable materialized view**, `logs/` stays the sole source of truth, every table is reconstructible by re-ingesting, and the supported total-recovery path is therefore just `rm logs/claude-proxy.db && pnpm --filter server ingest`. That invariant decides the rest. Nothing **authored** goes in — `logs/suggestion-status.json` and device settings stay JSON files, because a view that can be deleted may not hold the only copy of something no re-ingest could rebuild. The `.md` and `.request.txt` bodies (~1.2 GB/day, ~99% of the bytes) stay on disk untouched, with the DB holding nullable pointers plus an explicit `blob_evicted` flag, so "retention deleted the body, we kept the metrics" is a queryable fact rather than a dangling path. And **`proxy/` is not touched at all**: the proxy is load-bearing — if it breaks, Claude Code itself stops working — so the server does all ingest, which is a blast-radius decision rather than a dependency-purity one. Ingest runs on server start plus an `fs.watch` on the log directory, keyed on the sidecar filename stem, so running twice or dying halfway are both harmless; it reconciles each directory against its live listing, so a day the summary job archives moves its rows rather than duplicating them and a file retention deletes takes its rows with it. The schema is audit rows only in this slice — `request`, a normalized `request_tool` (the headline unlock: "which tools burn the most tokens" becomes a `GROUP BY`), `request_rate_limit`, and `ingest_watermark` — plus two tables the spec did not ask for and losslessness did: `request_skipped`, because the file readers count an unparseable file under `parseErrors` and a structurally invalid one under the digest's `skipped`, and a view that quietly dropped them could not reproduce those numbers; and explicit `*_present` flags, because a legacy sidecar with no `session` object and a current one whose every session field is null are different facts on disk. **Nothing reads from the database yet**, and the reason is the artifact this slice really exists to build. The file-backed and DB-backed readers sit behind one `SidecarSource` interface, and a **parity harness** replays every wired route (`/api/summary`, `/api/tools`, `/api/trends`, `/api/usage`) against the whole of `logs/archive` **both ways**, asserting the **full JSON is identical** — including object key order, which `JSON.stringify` preserves and the digest's model map inherits from read order — never a row count and never a summary. Legitimate diffs are permitted only as **named** normalizations that state the mechanism making them benign, and the list is **empty**: rather than sorting differently and compensating, the DB reader reproduces the file reader's filename-stem iteration order exactly, which is what makes tie-breaking in the tool table and the busiest-hour scan land the same way on both sides. A harness that cannot fail proves nothing, so one test corrupts a row on purpose and asserts the harness catches it. Against a hardlinked snapshot of this machine's real archive — 14 days, ~14,800 requests, ~506,000 tool rows — all four routes are byte-identical on every day with zero normalizations; the snapshot is what makes that reproducible, since replaying against the live directory races the proxy still writing to it. Alongside it, **shadow mode** (`SHADOW_DB=1`, off by default) serves from the files exactly as today and computes the DB answer in parallel, logging any disagreement; it is a strict observer, running after the response has gone out and swallowing every failure inside it, because a broken substrate must not be able to break a page. Routes flip per route, only once parity is green, and the log files keep being written throughout — recorded in [ADR 0004](docs/adrs/0004-adopt-sqlite-as-the-query-substrate.md) together with the hard constraint that `/revive` reads `logs/sessions/*.md` straight off disk with no API and no database, so a later stage must not stop writing those files until `/revive` has been taught the DB, on pain of the migration disabling its own recovery mechanism. The remaining five slices, and a resume runbook written for a cold agent, are in the [migration map](docs/wayfinder/map-sqlite-substrate.md).

- **A suggestion's `done` is read as a dated claim, so a fix that held stops looking like one that didn't.**
  - Buckets are frozen windows, so a rule that tripped on old sessions trips on them forever — "still tripping" was never evidence a fix failed.
  - Each flag's `updated` is compared against the window's session span, giving every row a `recurrence`: `historical` (the window predates the claim), `regressed` (the window was recorded entirely after it and tripped anyway), `mixed`, `none`.
  - The claim is rule-wide rather than per bucket: one `done` carries to every later window instead of needing one mark per bucket. Only `done` counts — `skipped` and undated flags never produce a regression.
  - `suggestions list` hides `historical` by default and prints the hidden count, so the suppression is visible rather than silent; `--recurrence <states>` and `?recurrence=` narrow explicitly.
  - The dashboard badges recurrence, counts `N regressed` in both headers, keeps regressed rows undimmed with a coral border, and folds `historical` into "hide resolved".

- **A Commands page that measures what each slash command costs to run** — a new **Commands** station studies the efficiency of the commands installed in `~/.claude/commands`: how many tokens each declared `## Step N` burns, what prompt drove the run, and where runs die. Capture is **passive** — every real invocation whose prompt envelope carries a `<command-name>` is a run, so there is nothing to tag and no harness to start — and a run is the top-level session rolled up with its whole subagent tree via the existing `linkAgentSessions`, with a nested command counting both as a segment of its parent and toward its own command's numbers. The feature exists in three levels: `/commands` lists installed commands with invocations, a cost sparkline, completion rate and last run; `/commands/:command` leads with a **run scatter over time** (x = when, y = total tokens, colour = outcome, size = turns) carrying vertical markers where the command file itself changed, above a stacked tokens-by-step bar that swaps to a single run on hover, a pattern-frequency table and the run list; `/commands/:command/:threadId` draws a **token-weighted step tree** with the declared steps as the spine, every turn, tool call and subagent hanging off its attributed step at a width proportional to its tokens, and a per-turn delta inspector that reuses `/api/context/message` to show the actual bytes a turn sent. Flags are a **facet** rather than a variant — `/task --sub` and `/task` are the same command, filtered — because splitting them would fragment a small sample into a smaller one. What makes any of this possible is a store: transcripts and captured request bodies live for roughly a day and only sidecars are archived, so an append-only, schema-versioned `logs/commands/runs.jsonl` distils each run while its raw material is still on disk, and the page reads only from there. One idempotent reconcile pass keyed by thread id upserts records, fired debounced on log-directory change so the page moves mid-run and again from the daily-summary job as a backstop — the same code path both times, safe to re-run. Because the store is the only source, **data accrues only going forward**: there is no backfill, and a fresh checkout shows installed commands with no runs, which is the honest state rather than an error. The design's real constraint is that **step attribution is a heuristic** — turns are placed by the agent's own narration and by nested sub-command boundaries, not by any marker the model emits — and the feature refuses to pretend otherwise. Each placement carries a confidence, the run detail states plainly how much is guessed ("53 of 64 transcript nodes were placed against a step, 3 of them anchored on something the run actually did"), and turns nothing could place go into a dashed, clearly-labelled **unattributed** gutter that the UI shows rather than hides. The attribution rules are isolated and unit-tested precisely so an explicit `STEP n/N` marker can replace them later without touching anything else. Nothing is normalised — no dividing by files touched or diff lines — because a size proxy would launder the raw distributions this page exists to show. The pattern catalogue is a small deterministic rule set firing on specific graph nodes and badged there, joined across runs for frequency ("seen in 13 of 18 `/task` runs"), not sequence mining; waste and rework are step-attributable counters in the run record (errored tool calls, duplicate reads, cache misses, no-op turns), and the existing per-session suggestions engine is **linked** on the run detail rather than rebuilt. Degradation is stated wherever it happens: a run whose transcripts have aged out says so instead of rendering zeros as fact, a command with no `## Step N` headings reads "none declared", a step never reached is labelled as such, and a schema bump leaves older records readable rather than crashing the page.

- **Usage meters that read Anthropic's own numbers** — the meters were live but wrong: the Overview showed a weekly window at ~142% while Claude Code's own `/usage` panel said 7%. Two independent causes, both fixed here. The first is the denominator. Anthropic returns `anthropic-ratelimit-*` headers on API-key traffic but not on subscription OAuth traffic — measured on this device, **0 of 12,929** captured sidecars carried a `rateLimit` field — so the "authoritative" header path never once fired, and every window fell through to a ceiling *learned* from history, which is only a lower bound on the allowance. Dividing by a floor reads far too high. So the proxy now polls `GET https://api.anthropic.com/api/oauth/usage`, the same endpoint that panel reads, and a window sourced from it is exact: Anthropic's percentage, Anthropic's reset instant, no ceiling guessed at and no coverage caveat. That call needs the user's OAuth token, which decided where it lives — **in the proxy**, where the token is already in memory to be forwarded, so only the resulting numbers ever reach disk and the credential is never written, logged, or put in a sidecar; `x-api-key` requests are ignored outright, the endpoint being OAuth-only. It polls on a fixed 60-second timer rather than per request or per SSE tick, because a busy session would otherwise hammer it hundreds of times a minute, and it writes `logs/usage-live.json`, which wakes the existing log-directory watcher so the Overview stays live over SSE with no new plumbing. Every failure mode degrades rather than breaks: a failed poll leaves the last good file alone, a reading older than five minutes stops being used as a percentage, an unrecognised `kind` is skipped rather than guessed onto the wrong meter, and with no token seen yet — a freshly-started proxy, before its first request — the whole thing simply falls back to the estimate. The second cause is the *numerator*, and it would have survived the first fix. Anthropic's weekly allowance is a **fixed** window resetting at a published instant, not a trailing seven days, so estimating over a trailing week sweeps up everything since last Friday and charges it to an allowance that reset on Saturday: on this device, 83.0M weighted units counted where 14.9M actually belonged, a **5.6× overcount**. The estimate now anchors to the real reset instant, and the anchor deliberately **outlives the reading it came with** — allowances reset on a fixed cadence, so an instant that has already passed is rolled forward by whole windows and a week-old poll still marks where the current window opened. Coverage for an anchored window is measured against the part that has *elapsed* rather than the nominal span, since a weekly window seven hours old with all seven hours on disk is completely covered, and judging it against 168 hours would read 4% and stamp `partial` on a complete count. With no anchor ever seen the window still falls back to trailing — wrong in a known direction rather than unknowably.

- **Deleting a job from the Jobs page, for real** — each row on **Jobs** gains a `Delete` that removes that job's whole directory from `~/.claude/jobs`: its state, its timeline and everything under its `tmp/`. Listing husks without a way to clear them was half a feature — they accumulate, nothing else reaps them, and the page that knows about them is the obvious place to act. It is an actual `rm -r` with no trash behind it, so the control is awkward in proportion: the first click *arms* the row (`Delete 4.2 MB?` / `Yes, delete` / `cancel`), only one row is armed at a time, and what was removed is stated afterwards — name, file count, bytes freed, path — above the table it just changed. A **running** job is refused: its daemon is still writing there, so the button is disabled up front rather than failing after the click, and the API answers 409 if something asks anyway. Backed by `POST /api/jobs/delete`, the one destructive route in the API and the one on the jobs surface that isn't a GET: it goes through the origin-checked write CORS the chat routes use rather than the read routes' `*`, since a wildcard origin on a delete would let any page open in the browser wipe the device's job history. Before removing anything it re-establishes the target from scratch — the same id validation and direct-child test, an `lstat` that refuses a symlinked job directory outright (removing the link would leave the target, following it would delete outside the root), and a realpath check that the directory is still a direct child of the realpath'd jobs root. The reply carries the refreshed listing as well as the removal, so the page never re-renders a row that is already gone.

- **A device-wide browser for `~/.claude/jobs`** — a new **Jobs** station lists every background job directory on the machine, whichever project it ran in: state, working directory, file count, size and last activity, newest first, with **Jobs / Running / Husks / On disk** tiles above it. A state word is badged by *tone* rather than matched exactly, so a vocabulary Claude Code extends degrades to `unknown` instead of being forced into a known bucket, and a directory with no readable `state.json` is listed as a **husk** rather than hidden — its job is gone, its scratch space is still on your disk. A job's own page shows what the state file says (the prompt it was given, agent and model, any PR it opened, what it had in flight at the last write) and presents the directory as a **folder tree**: directories before files, collapsible, opening on `state.json`, with `node_modules` and friends listed but never descended into and symlinks listed but never followed. Selecting any file opens it with a **Pretty / Raw** toggle — pretty re-indents and colours JSON, renders `timeline.jsonl` as badged state changes, strips terminal escapes and collapses carriage-return progress redraws out of a build log, numbers and colours source, and inlines a screenshot; raw is the bytes on disk, for the moment you suspect the pretty view of hiding something. Reading is confined to the job: the id and every path segment are validated, then the resolved path is `realpath`'d and re-checked against the job root, so a symlink an agent left in its own `tmp/` cannot become a way to read the filesystem; text is capped at 512 KB and a file whose bytes contain a NUL is reported as binary rather than shown as mojibake. Backed by `GET /api/jobs`, `GET /api/jobs/job?id=` and `GET /api/jobs/file?id=&file=` (`CLAUDE_JOBS` overrides the directory), with the shaping and the viewer transforms pure and unit-tested in `packages/core`.
- **Usage meters that configure themselves** — the meters previously needed `USAGE_LIMIT_5H` / `USAGE_LIMIT_WEEK` set per device, so a checkout without them showed only the note telling you to set them. They now fall back to a ceiling **learned from history**: the busiest *completed* window on record. The distinction matters twice over. Only completed windows count, because the one in progress is still filling and would peg every meter at 100%; and only windows the retained logs fully span count, because a window starting before the oldest retained request is a fragment, and admitting it would drag the peak down to whatever survived rotation. That history has to come from `logs/archive/<date>/`, which keeps full per-request sidecar triples — the live `logs/` directory holds roughly a day and cannot span even one completed week. The new `server/src/usage-history.ts` reads 28 days of live plus archived sidecars (four weeks leaves room for three completed weekly windows) and memoises the result for an hour, since thousands of sidecars is too much to re-read per request and the answer can only change when a window completes. What the feature refuses to do is the interesting half. A learned ceiling is a **lower bound on the allowance, never the allowance** — Anthropic never stated the limit, so the most that can be said is "at least this much was possible" — and the error runs in one direction only: dividing by a ceiling that is too low makes utilization read too *high*, so the meter overstates how close the account is and cannot invent headroom. The UI carries that rather than burying it: the window is marked `inferred` with the reasoning on hover, the status chip drops limit language for record language (`Below record`, `Near record`, `New record`), and the blurb calls passing the bar *new territory* instead of exhaustion — only the header path ever claims requests will be refused. A configured ceiling still wins outright, being a statement about the allowance where the learned figure is a guess at its floor, and `USAGE_LIMIT_ENV_SUFFIX` in core is now the single source of truth for those variable names so a blurb cannot suggest one the server doesn't read. Nothing is invented where nothing is known: a window with under one completed window of history is omitted as before, and a window kind with no traffic at all learns nothing rather than a ceiling of zero, which is how the Fable meter stays absent on plans that never touch it. Two documentation errors went with it — the archive keeps raw sidecars, not per-day digests as the feature doc claimed, and a proxy restart does **not** always produce `rateLimit` headers: Anthropic returns `anthropic-ratelimit-*` on API-key traffic but not on subscription OAuth traffic, so on such an account the header path never fires at all, which is exactly the gap the learned ceiling fills.

- **The Overview tracks the usage limits, live** — a meter per metered allowance (rolling **5-hour**, **weekly**, and **weekly Fable** where it applies), each with a sentence saying whether the current rate is comfortable, close to the ceiling, or on track to run out early. The meters and the day's digest now both arrive over SSE — `/api/usage/stream` and a new `/api/summary/stream`, both watching the log directory through the existing `serveSse` helper — so the page moves as the proxy captures requests instead of at the last reload. The bar's colour tracks the **pace**, not the fill level: a nearly-full bar late in a window is fine while a modest one early on may not be, so the tone comes from where the rate is heading, and a faint extension of the bar marks the utilization the rate projects to by reset. The projection is `utilization / elapsed` — the sustainable rate being to spend the whole allowance over the whole window — with `elapsed` derived from the reset instant, so a window resetting in 1h of 5h is 80% gone. Two sources feed it, in order. First, **Anthropic's own accounting**: the proxy already had `up.headers` in hand and now records every `anthropic-ratelimit-*` / `x-ratelimit-*` response header into the sidecar's new `rateLimit` field, which carries the real allowance and the real reset and so needs no configuration. Names are matched by *shape* rather than against a fixed list — one segment names the span (`5h`, `7d`/`week`), an optional one narrows it to a model family, the last names the field — so a renamed or newly-added window reaches the dashboard without a proxy change; resets are accepted as an ISO instant, epoch seconds, epoch milliseconds, or seconds-from-now; and only the newest request's headers are read, since an older reading understates everything since. Second, an **estimate** from logged tokens in weighted units (`input + output + cacheCreation + cacheRead × 0.1` — cache reads bill at about a tenth of fresh input, and `realInput` is avoided because it already sums the other three), measured against a ceiling from `USAGE_LIMIT_5H` / `USAGE_LIMIT_WEEK` / `USAGE_LIMIT_WEEK_FABLE` (`_`/`,` separators and `k`/`m` suffixes accepted). There is deliberately **no default ceiling**: Anthropic doesn't publish quotas as token counts, so a window with neither headers nor a configured limit is omitted rather than drawn against an invented denominator — and setting the Fable ceiling is what opts that meter in, which is how "optional based on plan" is expressed. The honesty of the estimate is the subtle part. `logs/` retains roughly the current day and the archive keeps per-day *digests* rather than raw sidecars, so an estimated **weekly** window routinely cannot see its own span — measured at 15% coverage on this device. Each estimated window therefore reports `coverage`, and below 0.95 the meter is labelled `partial` in amber with the blurb stating the real figure is higher, so a reassuring number never stands unqualified on incomplete data. Two more states refuse to read as reassuring zeroes: no requests captured at all emits no estimated window (0% would say "well within limits" when the truth is "nothing was observed"), and a trailing estimate gets its own vocabulary throughout — exceeding a ceiling the operator chose says so, and only the header path claims requests will be refused. The pace logic is pure and takes an injected `now`, so every threshold is covered by tests.

- **Every page loads in place** — all 21 query-backed views now show a placeholder shaped like the content they are fetching, instead of the one line of text every one of them shared. `QueryState`'s loading state was `<p className="muted state">Loading…</p>`: a 44px-tall stand-in for a screen of stat cards and a hundred-row table, so the document grew by thousands of pixels the frame the data arrived. `useRestoredScroll` exists because of exactly that — the router restores scroll while the route is still loading, and *"a document that short cannot hold the offset, so the browser clamps it away."* The placeholders are built out of the page's **own** classes — `.card`, `.card.stat`, `.table`, `.grid.stats` — with the shimmer placed *inside* those elements rather than replacing them, and each block is an `inline-block`, so the element holding it keeps its own strut and line box: a skeleton `.stat-value` is exactly as tall as a filled one and neither side names a pixel height, which is what stops the two drifting apart later. Where the eventual size is already known the skeleton uses it rather than guessing — the trends charts and tables reserve one bar and one row per day in the selected window, a suggestion bucket reserves ten session rows because a bucket is always ten sessions, and a session reserves seven stat tiles because the page fixes that count, not the data. A card is reserved with everything it carries and lands once, which is subtler than it sounds: an Overview tile holds a sparkline beneath its value and the per-request card holds a legend beneath its plot, so those boxes are reserved too — a tile that is the right height until its mini chart appears still pushes the page down when it does. The plot heights moved out of the stylesheet into exported `BAR_CHART_HEIGHT` and `SPARKLINE_HEIGHT` constants that the chart and its placeholder both read, so the two can't drift apart. And where a view is assembled from two queries — Overview's tiles draw their mini charts from the trends window, Skim's from a second day query — the skeleton is gated on both, so the row arrives complete rather than landing and then growing. The two pages that aren't a padded column get their own shape: the Sessions rail loads as ghost transcript rows into a grid column whose width is already fixed (the chat beside it is usable from the first paint, since only the rail ever waited), and the live graph draws ghost node boxes onto its canvas at the same `COMPACT` geometry and gaps the real snake lays out with. `useRestoredScroll` stays — reserving the height addresses the cause it was written for, but a skeleton's row count is a good estimate of the page's height rather than an exact match, so the explicit restore is still worth keeping. Shimmer blocks carry `aria-hidden` and each loading view announces itself once through a `role="status"` `.sr-only` element that is absolutely positioned, so it can sit inside a grid or flex container without becoming an item in it; the stylesheet's global `prefers-reduced-motion` rule already stops the animation, so the skeleton also declares a flat fill for that case, or the gradient would freeze mid-sweep and leave the blocks unevenly lit.

- **Switching a view keeps the view you already had** — the 7/14/30-day switchers change the query key, so React Query reported `isLoading` for the new key and the whole page reverted to `Loading…` even though a perfectly good previous window was drawn and only needed redrawing. Those pages now pair `useTransition` with `placeholderData: keepPreviousData`, so the drawn window survives the switch and dims via `.is-stale` while the next one arrives, and the switcher stays live throughout and reports `aria-busy`. The same treatment covers the other interactions that replace already-loaded content: re-sorting a table (Context size, Projects, Project memories, Request breakdown), flipping a document between Pretty and Raw (Session detail, Memory, Message, Tool schema), and hiding resolved suggestions. `useTransition` is used for what it actually does — without Suspense a transition's `isPending` does **not** track a fetch — so the two halves are reported separately and combined by the caller: `isPending` from the new `useTransitionState` hook covers the re-render, the query's own `isFetching` covers the request. Two pieces of duplication went with it: `Segmented` replaces the hand-rolled `.segmented` button group five pages repeated, and carries the `DAY_WINDOWS` and `PRETTY_RAW` option sets each of them had declared for itself.
- **A repository map, the failing shell forms, and refusals that are final** —
  `AGENTS.md` gains a **Repository map** so a session reads one section instead of
  re-deriving the layout: the four workspace packages, `server/src/server.ts` versus
  `api.ts`, the CLI entry points behind `pnpm --filter server suggestions`, the
  `logs/` transcript and sidecar layout, `my-command-tools verify`'s four gates, and
  the OKF bundle under `docs/`. **Shell command forms** records the invocations that
  kept failing — a relative `cd` when the working directory was a worktree,
  unquoted zsh globs aborting the command (`grep --include=*.ts` versus
  `rg -g '*.ts'`), and reading `@{u}` on a branch with no upstream — while the
  existing discovery bullet now covers confirming a *file* exists before `sed`-ing
  or reading it, and a new one hands a broad fan-out to a single search agent.
  **Environment-specific failures** names the two recurring ones with known
  recoveries: a 1Password signing prompt (retry the same commit once, never
  `--no-gpg-sign` around it) and `gh`'s GraphQL `must be a collaborator`, which is
  the wrong identity rather than a missing permission. Finally, a classifier refusal
  of a PR merge or a remote-ref deletion is now explicitly **final** — `gh api -X PUT
  .../pulls/N/merge` is `gh pr merge` and `gh api --method DELETE .../git/refs/...`
  is `git push origin --delete`, so neither counts as the one narrow retry the
  neighbouring bullet allows, and the two bullets were reworded to read together.

- **Repository-wide tool preconditions for agent edits and reads** — `AGENTS.md`
  now requires an existing target to pass through the `Read` tool before its first
  `Edit` — or the first `Write` that overwrites it — even when shell output or
  inherited context already exposed the file, and keeps paginated `Read` calls
  schema-valid by requiring integer `offset` and `limit` values while leaving the
  `pages` range a string. The read-before-write bullet names itself as the
  exception to the neighbouring rule against rereading an unchanged file, so the
  two read in either order. These guardrails extend beyond the task command to
  ordinary and god-mode sessions, where the same avoidable tool failures also
  recurred.

- **Repository agent guidance for efficient, ownership-aware workflows** — `AGENTS.md`
  now batches independent read-only discovery, avoids unchanged whole-file rereads,
  treats expected search misses as successful discovery, tracks how worktrees were
  created before choosing their cleanup path, and keeps classifier-sensitive Git
  branch operations to one exact action per shell call instead of weakening
  permission guardrails.

- **The live graph's node details hold the whole request** — the Node details drawer showed a step's one line and offered "Open request breakdown →", which landed on the request's front page and left you to find the turn. A step now records the `messages[]` position it was read from (`SessionNode.message`, `null` for a step read off a transcript, which has no such position), and the drawer uses it two ways: a **Request message** field fetches that turn whole from the same endpoint the Request breakdown's drill-down uses — labelled *#n of N · role*, folded behind the usual **Show all N characters**, and scrolling inside its own box when expanded rather than growing the drawer — and **Open this step's message →** deep-links to `/context/$file/message/$index`, that step's own message rather than the request it sat in. A step that paired with nothing in the captured request now says so, so a line that is short because it is a gist is legible as one instead of looking like the whole story. Node geometry is untouched: with **larger nodes** on, boxes keep their fixed 320×216 / 360×232 height and clip, so a step carrying twenty thousand characters reads in the drawer, not by stretching the canvas.

- **From an error to the turn that produced it** — every entry on a session's errors page (`/sessions/$id/errors`) now ends with **"View the full turn · message #n →"**, landing on the Request breakdown's Message details page for the request message that carried the failed `tool_result`. The errors page had been a dead end: the proxy records an error as one gisted `- ✗` line with no pointer back to a request, so reading the whole failure — the stack trace, the command, the arguments — meant opening the Request breakdown and hunting for the turn by hand. The handle is recovered rather than stored. `deriveRequestErrors` walks a captured request's `messages[]` for `tool_result` blocks flagged `is_error`, recording each one's array position (one entry per block, since a single user turn can return several failures), and `linkRequestErrors` matches those against the transcript's errors using `isSameStep`, the gist-aware comparison `mergeSessionNodes` already uses to recognise a truncated line's full original. The two are walked as subsequences rather than positionally, because a captured request is only the turns that were in flight when it went out: one sent before a failure never saw it, and one sent after a compaction has lost the failures before it, so each site claims the next transcript line it expands and a partial overlap links what it covers instead of nothing. Which requests get read is the part that decides whether the link appears at all: `resolveSessionRequests` (now shared with `buildSessionBreakdown`, which only wanted the peak) returns every capture for the session, and `requestsToScan` picks at most six — the peak, then an even walk along the session's timeline. Largest-first was tried first and is the wrong shape: the biggest bodies cluster at the end of a run, and once a session has compacted they are precisely the ones that dropped its early failures. On a real 193-request session the only body still holding the first error ranked **43rd by size**, past any budget worth spending, while the six-sample timeline walk found it — measured over the local `logs/`, the two strategies link 6 and 7 of the same 28 errors. Later requests only fill gaps the earlier ones left, so different errors can resolve to different requests and the scan stops as soon as every error has a home. Errors that budget can't account for — along with every error on a transcript carrying no session id, or whose captures have been pruned or won't parse — read a muted **"full turn unavailable"**; a missing link never fails the page.

- **A worktree comes up with the data it needs** — `scripts/bootstrap-worktree.sh` makes a linked worktree behave like the main checkout, which is what `/task` looks for when it creates one. `git worktree add` materializes only tracked files, so a fresh worktree had no `node_modules/`, no `.env`, and — the one that actually broke things — no `logs/`: `resolveLogDir()` defaults to `<repo>/logs`, so the dashboard rendered empty and `/health` reported the log directory unreadable, in a worktree that otherwise looked fine. The script finds the main checkout through `git rev-parse --git-common-dir` (no hardcoded paths, the same resolution `scripts/proxy-store-env.sh` already does for the transcript store), symlinks `apps/admin/.env`, `proxy/.env` and `logs/` from it, then runs `pnpm install --frozen-lockfile` — no generation step, because `@claude-proxy/core` is consumed as TypeScript source and there is nothing to generate. Symlinks rather than copies keep the main checkout the single source of truth: a `.env` edit or a newly written sidecar is visible from every worktree at once, and nothing drifts. It never overwrites a file the worktree already has, skips anything missing upstream with a note instead of inventing it, assumes no branch or base (so it is correct for a worktree cut off `main`, off a `--base`, or checked out onto an existing branch), and refuses to run from the main checkout, where every action would be meaningless or destructive. `.gitignore`'s `logs/` lost its trailing slash on the way: the pattern matched directories only, so the worktree's `logs` **symlink** showed up as untracked.

- **From a session into the live graph** — the sessions list gains a `graph →` link per row and a session's own page one beside its live indicator, both landing on `/sessions/graph?session=<threadId>`. The graph reads that search param instead of always opening on the newest session: it canvases the linked session's family, and centers the branch when what was linked is a subagent. Picking in the rail writes the param back, so the canvas survives a reload and the URL is shareable. The graph inspector's existing "Open transcript →" now has a way back.

### Changed

- **Documentation reflects the reporting clock and scoped write surface** — the OKF bundle now
  records that Overview, Trends, busiest hour, and Skim share the Eastern reporting day instead
  of UTC buckets; updates the dashboard, chat, config-inventory, and historical monorepo spec
  against the current API; and adds ADR 0003 to supersede ADR 0002's read-only-server constraint
  with the explicit chat and suggestion-status write allowlist. The root README and server
  package description now use the same architecture and point at the bundle's generated indexes
  instead of a stale design-doc count and removed path. All six queued concepts were then
  density-reviewed without changing their claims, clearing the bundle's `dirty` work queue.

- **Starting a session keeps you in the chat you started it from** — sending the first message on `/sessions` used to navigate to `/sessions/<chat-session-id>`, and that page then replaced its own URL with the thread id as soon as the proxy wrote the transcript. So a single send took you two hops out of the pane you were typing in, and the reply landed on a different page than the prompt: the chat client behaved like a redirect. The `navigate` on first send is gone, and the conversation now begins, continues and answers in place — `ChatSessionProvider` already lives above the router and holds the turns, so the pane was never the reason the page moved. What the detour was quietly providing is the thread id, which only reaches the pane on the reply, and an agent turn can run for an hour: the polling that resolved it moved out of the session page into a shared `useChatThread(sessionId, enabled)` hook, so the rail highlights the running session and the "open transcript" link works while the turn is still going, rather than after it lands. The session page keeps the same hook and the same URL canonicalization for a chat id someone opens deliberately — a bookmarked or shared link still resolves onto the transcript. `ChatConversation`'s `onSend` prop went with the navigation; it had no other caller.

- **The chat's empty state sits in the middle of the pane, and the footnote moved above the input** — "Start an agent session" was described as centered when it landed, but it rendered pinned to the bottom of the chat pane, just above the composer. `.chat-log--empty` did ask for `place-items: center`, and the stretched grid row would have honored it; what overrode it was `.chat-panel--fill .chat-log > :first-child { margin-top: auto }`, the rule that anchors a *short conversation* to the composer rather than letting it float at the top of the pane. The empty state is that log's only child, so it matched too and an auto top margin beat the alignment every time. The selector now excludes the empty log (`.chat-log:not(.chat-log--empty)`), which leaves the anchoring behavior intact for real turns and lets the centering apply where it was always meant to. The pane's block padding is evened out to `12px` for the empty case as well — the log's `8px` top against `22px` bottom would otherwise ride the "centered" state a visible few pixels high. Separately, the model / base URL / transport line, the sessions directory and the live indicator — the `chat-footnote` block — moved from *below* the input to directly *above* it, so where this chat runs is read on the way into the prompt instead of found after it; the rule's `margin-top` / `padding-bottom` became a `margin-bottom` to match the new direction. Both changes are in the shared `ChatConversation`, so the session detail page's chat card gets them too, and neither depends on `footnote` being passed — the session page passes none and renders as before.

- **The live graph can be read without opening a drawer** — a step's box was 168×64 with its label clamped to two lines, so anything longer than a short tool name was a gist you had to click to finish reading. The toolbar gains a **larger nodes** toggle that re-lays the canvas out at a roomier box size (a step goes 168×64, a session root 224×96 → 320×216 and 360×232) and lifts the title clamp from 2 lines to 8 — enough for the whole 160-character transcript gist at that width, so the text reads on the canvas itself. The geometry moved out of module constants into a `Sizes` preset threaded through `layout` / `layoutTree` / `layoutRun`, so the compact and roomy layouts are the same code path with different numbers rather than a second renderer. Toggling deliberately does **not** refit the view: a refit would scale the bigger boxes right back down and cancel out the reason for pressing it, so the zoom is left where it is and the boxes genuinely grow on screen. The **−** / **+** zoom buttons are gone to make room — they only did what the wheel already does better (⌘-scroll or pinch zooms about the cursor, rather than about the viewport center the buttons used), and that hint now hangs off **Fit**, which is the control that actually reframes. **Fullscreen** / **Exit** lost their text labels for lucide `Maximize2` / `Minimize2` icons, with the wording kept as the `aria-label` and tooltip; both toggles carry `aria-pressed` and light up in the signal color while their mode is on.

- **The sessions rail files things away, and the chat pane is just a chat** — the rail splits into **Active** and **Resolved**, each with its own scroller and a divider you can drag (or arrow) to give one of them more room; the split persists in `localStorage`. Hovering a card reveals a single control: a checkmark files it into Resolved, an arrow pulls it back and floats it to the top of Active. Resolved is stored as a mark plus a timestamp rather than a flag, so a session that takes another turn returns to Active on its own — the mark only holds while the transcript has been quiet since it was made. Search and a `+` for a new chat moved to the foot of the rail, inline with each other. On the chat side the page header is gone: the permission picker now sits in the prompt input's own toolbar the way a frontier chat client carries its controls, the posture disclaimers went with the header, "Chat vs Agent" is no longer a choice (a dashboard session is always an agent — the tools are the point), "Start an agent session" is the centered, larger empty state, and the model / base URL / transport line, the sessions directory and the live indicator became a footnote under the input.

- **Sessions is a chat client** — the page is now a full-height two-pane layout: a scrolling rail of transcripts on the left and the session you start from here filling the pane beside it. The rail (`SessionsSidenav`) sorts newest-first, filters by name / id / prompt / model, and renders 30 rows at a time, paging the next 30 from an `IntersectionObserver` sentinel as you scroll — `/api/sessions` returns the whole list in one payload, so the windowing is over what is *rendered*, which is what a machine with hundreds of transcripts pays for on first paint. The sortable table it replaces is gone; each row carries the name, a two-line prompt preview, a short age (`fmtAgeShort`) and model / tool / error chips. `ChatConversation` became a proper chat panel — avatar-and-bubble turns with the reader's own mirrored right, a transcript that follows itself down as turns land, a typing indicator for the prompt in flight, and a composer pinned below it — so the session page's live-chat card gets the same treatment. A short conversation anchors to the composer instead of the top of the pane, and under 900px the rail stacks above the chat.

- **A session's name wears the signal color, its id doesn't** — the title and the thread id swapped colors in the sessions list, so the primary label is the one that stands out and the mono id under it reads as plain text. The id keeps its link and hover underline, and a session with no name still shows its id in signal — `.session-id` only applies when a name sits above it.

### Fixed

- **`/clear` is no longer a command run, and no longer charged for the work that followed it** — the Commands index listed `clear` with eleven invocations, one of them at **$17.05 over 60 turns**, for a command that executes entirely in the CLI and never reaches the model. Nothing was miscounted: a run is any session whose opening prompt carries a `<command-name>`, and when a local command opens a turn the CLI sends its envelope as the *first* thing in that user message — so a session begun with `/clear` rooted on `/clear`, and every token the session went on to spend was booked against it. On this device that was true of eight sessions whose real command was typed in the same breath (`/mc -t feat/command-toolkit-cli`, `/revive`, `/review`, `/fb`, `/improve`), each of which lost its own run to a `clear` row, plus three where nothing followed the `/clear` at all and the row existed only to hold somebody else's cost. `parseCommandEnvelope` now walks the envelopes in order and takes the first **non-local** one, so those eight are recorded as the command that actually ran and the three are not runs at all. Locality is decided by **adjacency** and nothing else — the caveat text has to sit directly ahead of the envelope, the order the CLI emits across all eleven caveats in the captured logs — because that same caveat survives verbatim into compaction summaries, thousands of characters from an unrelated envelope it says nothing about. Each envelope's `<command-args>` is now read from its own block rather than from the first one in the prompt, which is what had been emptying these runs' prompts: `/clear` carries empty args, so the criteria the user actually typed were being discarded along with the label. That block ends at its **own closing tag**, not at the next envelope, because criteria quote envelopes constantly — a `/task` run whose criteria named `<command-name>/clean</command-name>` and `<command-name>/pr</command-name>` would otherwise have its whole 2,660-character args block cut at the first quotation and come back empty, inflicting the very symptom this entry is about on a run that had been read correctly. Because the store is append-only and cannot forget a line, a record whose thread no longer reads as a run is retracted by writing it back **retired** — `readCommandRuns` drops retired records and re-running retracts nothing further. Only a thread whose **opening prompt was actually read** is retracted: a transcript can outlive its `.state.json`, and the proxy records no `root` at all until it has one, so a sidecar that is missing, torn, or promptless is silence rather than testimony that the run never happened. Retired records are still carried forward through reconciliation, so a thread that reads as a run again keeps the turns that, once its sidecars are pruned, only the record still remembers.

- **The code caught up with the docs it was reconciled against** — the docs pass that preceded this one corrected every doc that was genuinely stale, which left a residue of the opposite kind: code that no longer did what its now-verified doc said. Seven of those closed here. **Skeleton loading** claimed every `QueryState` call site passed a skeleton, and six did not — Commands, Command detail, Command run, Jobs, and both queries on Job detail now reserve their own shape (stat rows, the 300px run scatter, the step tree, the file tree beside its viewer), built from the pages' own classes with no pixel heights, and the doc's call-site count is corrected from 21 to 27. **`server/src/api.ts` read as a binary file** to `rg` and `grep`, because a suggestion's map key was built as `` `${bucket}\0${id}` `` and the literal NUL made every search of that file silently miss — including the search for the function two items below. The separator is now a unit separator written as an escape, which no more appears in a number or an id than a NUL did, so key semantics and collisions are unchanged and `rg buildTools server/src/api.ts` matches again. **The read routes were not method-gated**: `POST /api/withheld` and its siblings answered 200 under the wildcard CORS that is only safe while those routes stay reads, and now answer 405 with `Allow: GET, OPTIONS`. The six documented POST routes keep their origin-checked CORS exactly as it was and still own their own methods — a foreign-origin POST to `/api/chat/sessions` is refused there with 403, not 405. **`UsageMeter` collapsed two pace statuses** into one label, so `on-pace` and `aggressive` both read `Near record` despite already carrying different tones; `aggressive` now says `Passing record`, which is what projecting past the record by reset actually means. **`requestsToScan` ordered a small candidate set largest-first**, which in a session whose bodies grow toward the end means reading only the tail — the same peak-then-even-timeline-walk the general path takes now runs for a set inside the budget too, with the timeline filling whatever reads the walk's own dedupe left spare. **`bucket-tool-schema-heavy` shipped `sources: []`** while its sibling rule named its evidence; `BucketBreakdownSummary` now carries `toolsSources` — the requests the average was taken over, heaviest schemas first, capped at ten — and the rule cites them. And **`PROXY_FILTER_INVENTORY`'s `task-tools` entry** described a proxy that no longer exists; it now states what `proxy/proxy.mjs` strips today: a regex anchored on the injected note's opening and closing phrases, cut from every text block, with an emptied block dropped, a message left with no content dropped, the blank-line run collapsed, and text nothing matched forwarded byte-for-byte. Two of the fixes carry new tests — that a small candidate set is not size-ordered, and that a read route refuses a POST over a real socket rather than a handler stub.

- **The weekly usage meter counts the whole week** — the Overview's estimated windows summed only the live `logs/` directory, which retains roughly the current day, so the weekly meter reported **3% of window** on this device: a seven-day figure drawn from about five hours of logs, comfortable-looking no matter how hard the account had been running. The archive that fixes it was already half-wired — `logs/archive/<date>/` keeps full per-request sidecar triples for weeks, and `learnCeilings` has always read four weeks of it to *infer the ceiling* — but nothing ever read it for the **count** that ceiling divides. `buildUsage` now unions the live directory with the archived days the windows reach into, so both halves of the estimate come from the same history. The interesting half is what had to change first. `coverage` measured the span back to the oldest surviving request, which reads a **hole in the middle** of a window as full coverage — and since the amber `partial` marking is what the span drives, backfilling alone would have swapped a visibly wrong number for an invisibly wrong one: this device's own archive is missing two days inside the current week. So coverage now counts the **days actually retained**, via a new `retainedDays` on `buildUsageLimits`; a day counts when its own archive directory is on disk, and because a reporting day can straddle `<date>` and `<date + 1>` the seam can *understate* coverage, which is the safe direction — it marks the window `partial` rather than presenting an incomplete count as a total. Days are the unit because rotation is day-granular, so a quiet stretch inside a retained day is genuinely quiet while a missing directory is a hole; day ends resolve through a new `dayStartMs`, which applies the zone offset twice so the two DST changeover days keep their real 23 and 25 hours rather than being forced to 24. Callers that pass no `retainedDays` keep the old span, which is all a caller with no retention map can honestly claim. Two smaller guards ride along: archived days are memoised individually for the process lifetime — `/api/usage/stream` rebuilds on a 600ms debounce and would otherwise re-read thousands of sidecar files per tick — while an *absent* day is deliberately **not** cached, since the archive job may simply not have run yet and a sticky miss would pin the gap in place until restart; and live and archived reads are deduped by source filename, so an archiver that copies rather than moves cannot double every request in the seam and inflate the very number the meters exist to report.
- **A session's own captures survive an evening start** — the three scans that floor themselves at a session's start (its Request breakdown and errors page via `resolveSessionRequests`, a suggestion bucket's per-session peaks, and the live graph's session-family request scan) derived that floor by slicing the raw UTC instant — `started.slice(0, 10)` — and handed it to `readSidecars`'s `since`, which compares against *reporting* days. Eastern runs behind UTC, so a session opened after 8pm Eastern carries a `started` whose UTC day is already tomorrow, and the floor then excluded every request that session ever made: the breakdown read zero captures, the bucket lost that session's peak, and the graph fell back to drawing the whole run from transcript gists. All three now derive the floor with `reportDay`, the same clock the filter compares against, so the scan starts on the day the dashboard actually buckets the session into — which also **supersedes the day-back widening** the graph's scan took in the entry below, since a floor derived on the right clock needs no slack to cover the offset. `REPORT_TZ` is unchanged: `America/New_York` already tracks EST and EDT as the date dictates. The filename prefilter inside `readSidecars` keeps its UTC slice on purpose — it selects a superset that each sidecar's own timestamp then narrows, and a UTC filename day is never *earlier* than its reporting day, so it cannot drop a sidecar the timestamp pass would have kept.

- **The live graph actually reads the captured requests** — the graph was documented as re-reading its step text from the Request breakdown, and in practice was drawing almost entirely from transcripts, where every line is gisted to 160 characters. Two independent defects, both silent. First, the scan window: the floor for which sidecars to read came off the family's earliest transcript `started`, which is a **UTC** instant, but the sidecar reader narrows by *reporting* day in `REPORT_TZ` (`America/New_York`) — so a session opened between midnight and ~04:00 UTC reports on the day *before* its own timestamp, the floor excluded every request it ever made, and the family resolved to zero captured requests. Measured on the local `logs/`: the same query returned `files=0` at the computed floor and `files=4382` a month earlier. The floor was first backed off a day to cover the offset; the entry above replaced that slack by deriving the floor on the reporting clock itself. Second, the merge: `mergeSessionNodes` advanced its position in the derived stream only on a match, so a single step the two streams worded differently desynced them permanently and every later step stayed at its gist — equal-length 132/132 streams matched only 100, and a 45/45 pair matched 17. It now walks both sides and re-syncs on a mismatch, searching outward along growing diagonals for the nearest pairing that skips fewest steps on either side within a 24-step window; drift is counted in steps because its cause is whole turns one side holds and the other doesn't. Past the window the transcript carries the rest alone, so the graph still degrades rather than breaks. Over the real log directory the two fixes take step coverage from no thread resolving at all to **355 of 358 steps (99%)** read from the captured request.

- **The Request breakdown holds its place when you come back to it** — clicking a row deep in a request's "Messages by size" table and then going back no longer dumps you at the top of the page to scroll down and find your place again. The router had no scroll restoration at all (`createRouter({ routeTree })`), so every arrival — including a Back — started at offset 0; `scrollRestoration: true` turns on TanStack's per-history-entry snapshot, which also covers the other drill-downs and any scroller the page owns, while a forward navigation still starts at the top. That option alone is not enough for a page whose body arrives with a query: the router restores the offset the moment the route renders, which on a cache miss is the loading state, and a document that short cannot hold the offset, so the browser clamps it away. `useRestoredScroll` re-applies the remembered offset once the breakdown's query has resolved and the tables are on screen, and no-ops on a first visit, where the cache has no entry for the location.

- **The day rolls over at local midnight, not 20:00** — every day-bucketed Overview and Trend value is now bucketed in Eastern (`America/New_York`, so EST or EDT as the date dictates) instead of on the sidecar timestamp's raw UTC prefix. An evening's work used to jump to "tomorrow" as soon as UTC crossed midnight, so the Overview both counted the *previous* evening's requests and hid the current one's — on a sample day that was 676 requests wrongly included and 265 wrongly missing. Because the proxy names files in UTC, one reporting day now straddles the filenames `D` and `D+1`: the filename filter selects a superset and each sidecar's own timestamp narrows it exactly, and `readArchivedDay` merges the two UTC archive folders a reporting day spans. Busiest-hour is computed and labelled in the same zone (`17:00 EDT`, not `17:00 UTC`), and the Overview date line and the "By day" tables name the zone they're in. The Skim trend buckets in the same zone, so its days line up with the window that selects them. Digests already finalized into the archive keep the UTC buckets they were written with; raw archived sidecars take precedence over them, so this only shows on days whose sidecars have been pruned.
- **An unsent chat draft survives navigating away** — what you typed into the Sessions page composer but never sent is no longer discarded the moment you click through to a session, the graph, or anywhere else. The draft moved out of `ChatConversation`'s component state (which unmounts with the page) and into `ChatSessionProvider`, which already sits above the router so the turn log, pending prompt and Stop button outlive the page they were started on — the input now travels with them. It still clears where clearing is the point: `send` empties it on submit rather than on success, since the prompt is already on screen as a turn, and "New chat" clears it along with the session it belonged to.

- **Sessions are named, not numbered** — a transcript now headlines what it is instead of its thread id, which is what 92 of 100 local transcripts showed. Only a session Claude Code titled itself carried a name, and it only titles interactive chats: a dashboard-started run is headless (`claude --print`) and a subagent shares its parent's session id, so neither is ever titled. `deriveSessionName` condenses the opening prompt into a short sentence-case name for those, `sessionName`/`sessionDisplayName` set one fallback order (CLI title → derived name → prompt → id) that the listing, the graph, the detail page and a suggestion's sources all read, and the id stays as the mono sublink. Two title-linking bugs went with it: a title landing on the *first* thread whose opening prompt matched left a second thread with the same prompt permanently nameless (it now goes to the most recently active **untitled** match, and never displaces a title already written), and titles were only ever matched against in-memory threads, so a proxy restart dropped them — an unclaimed title now waits in a `.pending-titles.json` sidecar and a title arriving for a thread that exists only on disk is written to it directly.

## 07-26-2026

### Added

- **Suggestion status flags in the dashboard** — the Advice pages now show the flags the API and CLI already wrote. A bucket's drill-down badges each suggestion `Done`/`Skipped`, dims the ones acted on, and carries a `Pending / Done / Skipped` control that records the flag through `POST /api/sessions/suggestions/status` and re-reads the list rather than patching the row; `Pending` is the undo. A "hide resolved" toggle folds away what is finished, and the Advice bucket list marks resolved suggestions and counts how many are still open per window. Breakdown-derived suggestions stay unflagged — the status store is keyed per bucket suggestion and has no row for them. (#67)
- **Interruptions in the live session graph** — a run that was cut off now shows the severed step with a coral ring and torn edge, and lays the resumed steps out in an inset dashed "side trail" labelled with why it stopped; the toolbar counts interruptions, the legend gains a swatch, and the inspector gains "Cut off" / "Resumed after". Both sources are covered: Claude Code's Esc (`splitInterruption` strips the `[Request interrupted by user]` marker the CLI prepends) and the dashboard's Stop button (`recordInterruption` appends `- interrupted: <why>` to the thread's transcript, since the child is killed before anything reaches the wire). (#63)
- **Session suggestions carry a status flag** — every suggestion is `pending` until marked `done` or `skipped` (with an optional note), keyed by `(bucket, suggestion id)` so the flag survives the recomputation that happens on every load, and stored in `<logDir>/suggestion-status.json` beside the transcripts it describes. A lean `GET /api/sessions/suggestions/status?range=2-9&status=pending` lists one row per suggestion so an agent can find outstanding work across a range of buckets without pulling each drill-down, `POST` to the same path records flags, and `pnpm --filter server suggestions list|mark` does both from the command line with no server running. `&detail=1` (`-d`) adds each suggestion's detail, evidence and sources when the caller is about to act on them. (#66)
- **Start a session lands on that session's page** — sending the first dashboard chat now navigates to `/sessions/$id` addressed by the chat session id (a uuid; thread ids are 16 hex chars), where the page polls a new read-only `GET /api/chat/thread?sessionId=` until the transcript exists and then replaces the URL with the thread id. The chat itself moved above the router (`ChatSessionProvider`) so the turn log, Stop button and pending prompt survive the navigation, and renders as a section between the stats and the Transcript card — the prompt in flight shown as a turn immediately, the reply when it lands, and an input to carry on. The running-turn Stop bar hides when this tab owns the turn. (#65)
- **Untruncated graph steps** — live session graph nodes now derive their text from the captured request body's `messages[]` (`deriveSessionNodes`) and merge it over the transcript stream, so prompts and command lines no longer arrive cut off at the transcript's 160/60-char gists; the node inspector became an expandable drawer. (#60)

### Changed

- **Dashboard chats default to `bypassPermissions`** — the start-a-session form and `DEFAULT_PERMISSION_MODE` now open on the mode a dashboard `/task` needs to finish its git writes; narrower modes are the opt-in for turns that shouldn't act. (#57)

### Fixed

- **Chat turns time out on silence, not elapsed time** — a turn is now bounded by an idle timeout re-armed on every stdout/stderr chunk plus a separate absolute ceiling, instead of one 300s wall-clock cap that SIGTERMed healthy 27-request agent loops mid-tool-call. (#56)
- **Rail collapse toggle placement** — the sidenav collapse button now occupies the `admin` brand pill's slot in the rail head instead of overflowing it. (#58)
- **Clicking the graph dismisses the details drawer** — a stationary press on empty canvas now clears the node selection on pointer-up (a 4px slop threshold keeps real pans from counting as clicks), so clicking anywhere off the nodes closes the drawer the way a backdrop would; clicking another node still switches to it, and Esc already closed it outside fullscreen. (#64)
- **Scroll pans the Live Graph** — a plain wheel or two-finger trackpad scroll now pans both axes (shift-wheel pans horizontally) instead of zooming on every wheel event; ⌘-scroll zooms about the cursor at the old 1.12 notch step and trackpad pinch zooms continuously, with the +/− buttons carrying a tooltip that names the modifiers. (#62)

## 07-25-2026

### Added

- **Dashboard chat sessions** — start a Claude Code session from the dashboard in `chat` (read-only) or `agent` mode, with `GET /api/chat/running` listing turns in flight and a `RunningChatBar` on the session page that can stop a turn started in another tab. (#48)
- **Peak context per session** — session detail gained a Peak context tile that drills into the Request breakdown of the largest request that session sent, backed by a pure `sessionContextPeak` and `GET /api/sessions/breakdown`. (#53)
- **Collapsible side rail with station icons** — the rail collapses to a 64px icon-only strip (lucide icons on all 12 stations), persisting through `localStorage` and gated above 861px. (#49)
- **`scripts/proxy-store-env.sh`** — resolves `CLAUDE_PROXY_STORE` from the checkout itself rather than a hand-maintained path, with `--setup` / `--check` / `--hookup` modes and a zellij preflight that surfaces an unconfigured shell. (#47)

### Changed

- **Overview station icon** — swapped from lucide `LayoutDashboard` to `Monitor`. (#54)
- **Docs bundle reconciled with the code** — repaired five unparseable wayfinder tickets (missing `type`, unquoted `wayfinder:prototype` label), fixed the README that prescribed the broken YAML, regenerated all six `index.md` listings, and added feature docs for already-shipped work. (#46)

### Fixed

- **Trends span the full window** — `buildTrends` now reads archived raw sidecars; the previous path looked for a `digest.json` that is never written anywhere, so every archived day resolved to `null` and 30-day views silently collapsed to the ~2 days still in `logs/`. (#52)
- **Session graph side panels keep their own wheel** — scrolling the expanded rail or node inspector no longer zooms the graph, with `overscroll-behavior: contain` and `touch-action: pan-y` on both. (#45)
- **Stable dashboard page width** — standard pages fill their workspace at every size and cap at 1200px, so headers stop growing after data loads; the graph keeps its full-bleed layout. (#51)
- **1-based message numbering** — the Request breakdown `#` column, drill-down heading, breadcrumb, Position tile, and pager all read from 1; route params and the API stay 0-based. (#55)

## 07-24-2026

### Added

- **Live session streaming over SSE** — the Sessions list and per-session transcript update in real time via `GET /api/sessions/stream` and `/api/sessions/session/stream`, backed by a generic `serveSse()` helper (snapshot + debounced `fs.watch` updates, heartbeat, teardown) and a `useLiveQuery()` hook. No new dependencies. (#39)
- **Per-metric trend charts on Overview** — every stat card carries a sparkline of that metric's real per-day history, a 7/14/30-day window selector, a hover popover of daily values with linked chart-node/row highlighting, and a per-metric drill-down. (#40)
- **Session titles and subtitles** — transcripts now capture the thread's reminder-stripped first user message (subtitle) and the CLI's own auto-generated chat title, which the proxy recovers from the separate titling request and links back to the thread. (#41)

### Changed

- **Live session graph redesign** — the graph is full-bleed with a floating toolbar, shows one session at a time as a viewport-adaptive boustrophedon fold, and gained a collapsible left session rail. (#37)
- **Graph fits the space the rail leaves free** — a `freeArea()` helper measures the rail live (272px expanded / 38px collapsed / 78% on narrow viewports) so fitting and focus-centering stop hiding the session behind the overlaid list. (#43)

### Fixed

- **Rail collapse actually collapses** — the old 18px peek lip plus `:hover` reopen was a no-op for a cursor that hadn't moved; the rail now narrows to a 38px strip with an explicit reopen affordance that is keyboard- and touch-reachable. (#42)
- **Subagent branches on the graph** — `spawnAgentType()` / `isAgentSpawn()` / `linkAgentSessions()` reconstruct the parent/subagent tree, which nothing on the wire names, by matching each spawn against the session family's transcripts in start-time order. (#42)

## 07-23-2026

### Added

- **Passive per-agent session transcripts** — the proxy maintains append-only `logs/sessions/<threadId>.md` handoff artifacts distilled per turn into `task` / `decided` / `tool` / `error` / `done` lines, keyed by conversation-root thread and filtered so one-shots never get a file. Records a tool name plus one key arg only — never schemas, payloads, or the system prompt. (#33)
- **Sessions browser** — a Sessions page listing every transcript newest-first with model and task/tool/error counts, plus a detail page with stat tiles and a Pretty/Raw viewer, over new `GET /api/sessions` endpoints and a pure `parseSessionTranscript()`. (#34)
- **Per-session Errors page** — errored tool results render as coral, left-accented transcript rows and get a dedicated `/sessions/$id/errors` route linking each error back to its task and probable tool call. (#36)
- **Hooks & Plugins inventory** — a config-inventory page over `~/.claude/settings.json` `hooks` and `enabledPlugins` with per-launch-alias load expectations (`native` / `not-loaded` / `unverified` / `expected`). Explicitly not a live tracker: hooks never reach the API. (#28)
- **Project memories viewer** — Projects list, project detail, and memory detail routes with a Pretty/Raw viewer and sortable File/Size/Modified columns, over new server-side project discovery. (#30)
- **Injected-reminder stripping + Proxy filters page** — the proxy removes harness-injected reminders (starting with the "task tools haven't been used" nudge) that no setting can suppress, and `PROXY_FILTER_INVENTORY` renders the canonical filter list in the dashboard. (#35)
- **Message pagination** — a Previous/Next pager on the message details page walks adjacent messages of the same request, disabled at each end. (#31)
- **Breadcrumb trails** — a reusable `Breadcrumbs` component replaces the single back link on five drill-down routes. (#32)
- **Favicon and per-page titles** — a ClaudeProxy brand-mark favicon and route-declared `staticData.title` synced into `document.title`. (#38)

### Changed

- **`EndConversation` stripped from forwarded requests** — Claude Code exempts a few protected tools from `permissions.deny`, so their schemas keep shipping every turn; the proxy now removes `WITHHELD_TOOLS` at the single chokepoint, re-serializing the body only when something is actually removed. (#29)

## 07-22-2026

### Added

- **All context requests, sortable** — `/context` lists every request in the window (newest-first by default, sortable on When/Model/Real input/System/Tools/Size) instead of a fixed 10-largest list; `summarizeContext` returns an `entries` array alongside its aggregates. (#22)
- **Net effective alias posture** — `computeAliasPosture` derives each `claude*` alias's real withheld tool set from settings precedence, parsing `--setting-sources` and inline `--settings` overrides, so aliases stop reporting "withholds nothing". (#27)

### Changed

- **Local-timezone timestamps** — analytics timestamps render in the viewer's timezone via `Intl` (no date library); UTC-bucketed aggregates keep their UTC labels rather than being mislabeled. (#24)
- **Trends plots the whole window** — the line chart shows every day in the 7/14/30-day range instead of paging three days at a time, and the default window dropped from 14 to 7 days. (#25)
- **Trends reads the durable digest archive** — `buildTrends` merges live `logs/` digests with one small archived `digest.json` per past day, so charts fill the window instead of collapsing to the ~2 days still on disk. (#26)

### Fixed

- **Stats/card spacing** — a `.grid + .card` rule restores the missing gap where a stats row butted against the following card on the request-breakdown page. (#23)

## 07-21-2026

### Added

- **Context size analytics** — a page answering what the average context is, when it peaked, and why, measured on `tokens.realInput`, with `summarizeContext` / `analyzeRequestBody` helpers, `GET /api/context` and `/api/context/detail`, and a traversal-guarded `file` handle. (#18)
- **Message drill-down** — `/context/$file/message/$index` renders any message's role, size, and full content from the parsed body, so it resolves even when the raw-JSON view was truncated. (#19)
- **Pretty/Raw toggles and a tool drill-down** — message and per-tool detail subpages gained word-wrapped Pretty/Raw views, and the breakdown leads with messages. (#21)
- **Launch aliases section on Not added** — a pure `parseLaunchAliases(rc)` extracts `--disallowedTools` from `claude*` shell functions and aliases, surfacing tools stripped per launch. Declarative by nature: the flag never reaches the proxy, so it can't be traffic-verified. (#17)

### Fixed

- **Readable full-message text** — `.rawjson` uses `var(--text)` instead of near-black `var(--ink)` on a dark background. (#20)
- **Contained breakdown tables** — `overflow-wrap: anywhere` on cells and `nowrap` on headers/numerics keep long `mcp__…` tool names from blowing out the layout. (#21)

## 07-20-2026

### Added

- **Non-streaming usage capture** — `decodeResponse` falls back to a single JSON message's top-level `usage`, so non-streaming responses (e.g. sonnet-5 safety-classifier calls) log real token counts instead of 0. (#14)
- **Per-session identity in sidecars** — each audit record carries a `session` block (session id, app, user agent, account/session/device from `metadata.user_id`), so spend attributes per session and subagent instead of being guessed by timestamp. (#14)
- **`disable*` settings on Not added** — boolean schema-stripping settings such as `disableWorkflows` and `disableArtifact` are now resolved to tools and verified against recent traffic alongside `permissions.deny` rules. (#16)
- **Device setup docs** — README covers running the proxy on this device (`PORT=8036`, `pnpm zellij`) and the zshrc alias routing the `claude` CLI through it. (#15)

### Fixed

- **Model attribution** — `model` falls back to the response model when the request body omits it, clearing "unknown" model rows. (#14)

## 07-19-2026

### Added

- **Skim cache dashboard** — aggregate hit rate, saved tokens and spend, and recurring request shapes from audit sidecars, over daily and trend skim APIs, with each shape's user request text recovered from its sibling request log. (#11)
- **Wayfinder project skill** — `.claude/skills/wayfinder/SKILL.md` adapted for this repo (verify via `pnpm typecheck && pnpm test && pnpm build`, docs under `docs/specs/` and `docs/adrs/`). (#13)
- **Cacheability research and guardrails** — analysis of 1,787 captured request bodies putting the byte-exact repeat floor at ~1.1% (~0.6% for the streamed `/v1/messages` subset the skim caches) and collapsing ~99% of traffic into ~63 recurring shapes, plus a decision doc fixing the cache's default-off posture, kill switch, never-cache list, and staleness bound. (#10, #9)

### Fixed

- **Zellij services stop with the terminal** — `pnpm zellij` runs through a terminal-scoped launcher with a unique session name and quits the server on forced closure, so watchers and Vite processes stop being orphaned while unrelated sessions survive. (#12)

## 07-18-2026

### Added

- **Opt-in skim response cache** — `proxy/skim.mjs` short-circuits byte-exact repeat streamed `/v1/messages` requests by replaying the stored SSE reply with zero upstream calls. Off unless `SKIM_CACHE` is set; keyed on `sha256(rawBody)` with a `SKIM_TTL_MS` TTL, and reported in a `skim` block on each sidecar. Distinct from Anthropic's prefix cache — this caches model output, so hits work across sessions. (#7)
- **Tokens-per-request chart** — a multi-series line chart of real input, output, and cache tokens per request on Trends, via a reusable themed `SeriesLineChart` (recharts). (#5)

### Changed

- **Side-nav layout and telemetry-console redesign** — the horizontal topbar became a left side rail styled as "the line" the proxy taps, with a signal palette (teal/amber/coral), a mono/sans split that encodes data vs. prose, and a live health pulse; folds to a top bar under 860px. (#6)

## 07-16-2026

### Added

- **Monorepo and admin dashboard** — the single-file proxy repo became a pnpm workspace: `proxy/` (moved intact, still zero-dependency), `packages/core` (typed sidecars, digests, cost estimation, pluggable advice), `server/` (read-only Node API over the logs), and a TanStack admin dashboard for usage, trends, and advice. Verified against 2,381 real sidecars. (#1)
- **"Not added" page** — reports and verifies device-wide withheld tools against captured traffic, after the proxy's own audit found 13 tool schemas riding along ~1,300 times each in 14 days. (#4)

### Changed

- **Proxy default port** — dev workflow and docs moved the proxy to `PORT=8036`, documenting that proxy and server both read a bare `PORT` and must be set per process. (#3)

## 07-15-2026

### Added

- **Zellij dev layout** — `.zellij/claude-proxy.kdl` opens proxy, server, and dashboard panes in one window via `pnpm zellij`. (#2)
